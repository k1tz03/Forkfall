name: fusible_core
description: Deterministic, data-driven rules engine for FUSIBLE (a football Reigns-like).
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.6.0
resolution: workspace
dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
