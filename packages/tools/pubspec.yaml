name: fusible_tools
description: Content build, lint and Monte-Carlo simulation tools for FUSIBLE.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.6.0
resolution: workspace
dependencies:
  fusible_core:
    path: ../core
  yaml: ^3.1.2
dev_dependencies:
  lints: ^4.0.0
